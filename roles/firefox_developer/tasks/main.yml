---

- name: Check if already installed
  stat:
    path: '{{ apps_path }}/firefox/firefox'
  register: ffd

- name: Download Firefox Developer Edition from Mozilla
  get_url:
    url: https://download.mozilla.org/?product=firefox-devedition-latest-ssl&os=linux64&lang=en-US
    dest: /tmp/firefox-dev-edition.tar.gz
  when: not ffd.stat.exists

- name: Extract archive
  unarchive:
    src: /tmp/firefox-dev-edition.tar.gz
    dest: '{{ apps_path }}'
  when: not ffd.stat.exists

- name: Install .desktop entry
  template:
    src: '{{ role_path }}/files/firefoxDeveloperEdition.desktop'
    dest: '/home/{{ ansible_user_id }}/.local/share/applications/firefoxDeveloperEdition.desktop'
    owner: '{{ ansible_user_id }}'
    group: '{{ ansible_user_id }}'
    mode: '0755'
  when: not ffd.stat.exists
