---

- name: Install Packages
  apt:
    name: "{{ item }}"
  loop:
    - htop
    - vim
    - tmux
    - build-essential
    - tree
    - python-pip
    - python3-pip
    - docker
    - docker-compose
  pip:
    name: pexpect

